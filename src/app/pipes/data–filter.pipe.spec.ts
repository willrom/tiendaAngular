import { Data–filterPipe } from './data–filter.pipe';

describe('Data–filterPipe', () => {
  it('create an instance', () => {
    const pipe = new Data–filterPipe();
    expect(pipe).toBeTruthy();
  });
});
